# MechMind frontend — משתני סביבה
# העתק ל-.env.local לפיתוח, או הגדר ב-Vercel/Render לפרודקשן.

# כתובת ה-backend. בפיתוח מקומי השאר ריק — Vite עושה proxy ל-localhost:8000.
# בפרודקשן: הכתובת המלאה של שירות ה-backend, למשל https://mechmind-backend.onrender.com
VITE_API_URL=
